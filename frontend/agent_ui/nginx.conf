# frontend/agent_ui/nginx.conf (Cleaned-up for conf.d directory)

server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # The SPA magic:
    location / {
        try_files $uri $uri/ /index.html;
    }
}